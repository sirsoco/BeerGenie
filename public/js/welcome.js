// function to retrieve Data
//Even listener for calling route for beerId





$(document).on('click','button.search','/search.html' );
$(document).on('submit', '#form-search','/search.html' );


//function listBeer() {
   // console.log('searching for beer🕵🏿‍♂️...')
    // container to hold favorite


//var routes = require('../api-routes')
//function listBeer(id) {

    // if a beer is availabe make an axios call
   // if(id)
// routes.

    //call for axious route

var storedBeer = JSON.parse(local.Storage.getItem("Dish")) || [];